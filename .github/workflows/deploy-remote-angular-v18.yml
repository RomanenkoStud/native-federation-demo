name: Deploy Remote Angular v18

on:
  push:
    branches: [main]
    paths:
      - 'remote-angular-v18/**'
      - '.github/workflows/deploy-remote-angular-v18.yml'
  workflow_dispatch:

concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: remote-angular-v18/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: remote-angular-v18

      - name: Build
        run: npx ng build
        working-directory: remote-angular-v18

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: remote-angular-v18/dist/notifications/browser
          destination_dir: remote-angular-v18
          keep_files: true
