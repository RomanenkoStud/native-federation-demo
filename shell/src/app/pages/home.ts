import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  template: `
    <h2>Welcome to Team Dashboard</h2>
    <p>This is the Angular shell application. Navigate to <strong>Projects</strong> to see a React micro frontend loaded via Native Federation.</p>
  `,
})
export class HomePage {}
